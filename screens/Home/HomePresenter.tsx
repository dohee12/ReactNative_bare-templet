import { Text, View } from "react-native";
import styled from "styled-components";

const Container = styled(View)``;
const Title = styled(Text)``;

const HomePresenter = () => {
  // logic xxxx
  // Rendering
  return (
    <Container>
      <Title>Home 메인 화면 입니다.</Title>
    </Container>
  );
};

export default HomePresenter;
